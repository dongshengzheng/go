<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fish.idle.service.modules.jsdd.mapper.InterpretationMapper">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
		id, works_id AS worksId, user_id AS userId, `type`, parent_id AS parentId, score_demanded AS scoreDemanded, `status`, deal_date AS dealDate, picture, follow_count AS followCount, create_date AS createDate, update_date AS updateDate, create_by AS createBy, update_by AS updateBy, del_flag AS delFlag
	</sql>

    <!--根据作品id查询所有诠释,诠释包含诠释图片-->
    <select id="interpretationContainImages" resultMap="InterpretationMap">
        SELECT inter.* from dd_interpretation inter
        WHERE inter.works_id=#{0}
    </select>

    <select id="byIdContainImages" resultMap="InterpretationMap">
        SELECT inter.* from dd_interpretation inter
        WHERE inter.id=#{0}
    </select>

    <resultMap id="InterpretationMap" type="Interpretation">
        <result property="id" column="id"></result>
        <result property="userId" column="user_id"></result>
        <association property="appUser" column="user_id"
                     select="com.fish.idle.service.modules.sys.mapper.AppUserMapper.searchMyInfo"/>
        <collection property="imagesList" column="id" ofType="Images"
                    select="com.fish.idle.service.modules.jsdd.mapper.ImagesMapper.selectByInterId"/>
    </resultMap>

</mapper>